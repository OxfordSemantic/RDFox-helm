{{- $profile := index .Values.persistenceProfiles .Values.serverParameters.persistence }}

{{- with (and $profile $profile.storageClass) }}
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
    name: {{ .name }}
{{ omit . "name" | toYaml | nindent 0 }}
{{- end }}
